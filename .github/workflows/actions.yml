name: Simple Action
on: [push]

jobs: 
    simple-action:
        runs-on: ubuntu-latest
        steps:
            - name: simple JS action
              uses: actions/hello-world-javascript-action@main
              with: 
                who-to-greet: Mario